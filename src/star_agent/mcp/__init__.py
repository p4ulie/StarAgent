"""MCP transport — exposes the Star Citizen knowledge base to MCP clients.

A second thin transport over the shared ``agent``/``rag`` core (the first is
Discord). Note: absolute imports of the external ``mcp`` package resolve to the
top-level package, not this one.
"""
