"""Offline, re-runnable ingestion pipeline that (re)builds the knowledge base.

Run with ``star-agent-ingest`` (or ``python -m star_agent.ingestion.build_index``).
Never invoked from the Discord event loop.
"""
